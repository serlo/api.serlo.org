type SetThreadStateNotificationEvent implements AbstractNotificationEvent {
  id: Int!
  instance: Instance!
  date: DateTime!
  actor: User!
  thread: UnsupportedThread! #TODO: Align with real Thread
  archived: Boolean!
}
