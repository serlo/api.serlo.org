type GroupedExercise implements AbstractUuid & AbstractEntity & AbstractExercise {
  id: Int!
  trashed: Boolean!
  instance: Instance!
  alias: String
  date: DateTime!
  license: License!
  currentRevision: GroupedExerciseRevision
  solution: Solution
  exerciseGroup: ExerciseGroup!
}

type GroupedExerciseRevision implements AbstractUuid & AbstractEntityRevision & AbstractExerciseRevision {
  id: Int!
  author: User!
  trashed: Boolean!
  date: DateTime!
  groupedExercise: GroupedExercise!
  content: String!
  changes: String!
}
