type Navigation {
  data: JSON!
  path: [NavigationNode!]!
}

type NavigationNode {
  label: String!
  url: String
  id: Int
}

extend type Mutation {
  _setNavigation(data: JSON!, instance: Instance!): Boolean
}
