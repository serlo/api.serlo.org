interface AbstractUuid {
  id: Int!
  trashed: Boolean!
  threads(
    after: String
    before: String
    first: Int
    last: Int
  ): ThreadsConnection!
}

type Query {
  uuid(alias: AliasInput, id: Int): AbstractUuid
}
