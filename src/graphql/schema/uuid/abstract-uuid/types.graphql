interface AbstractUuid {
  id: Int!
  trashed: Boolean!
  uuidEvents(
    after: String
    before: String
    first: Int
    last: Int
  ): AbstractNotificationEventConnection!
}

type Query {
  uuid(alias: AliasInput, id: Int): AbstractUuid
}
