interface AbstractEntity {
  # extends AbstractUuid
  id: Int!
  trashed: Boolean!

  date: DateTime!
  instance: Instance!
  alias: String
  license: License!
}

interface AbstractEntityRevision {
  # extends AbstractUuid
  id: Int!
  trashed: Boolean!

  author: User!
  date: DateTime!
}
