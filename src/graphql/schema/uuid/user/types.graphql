type User implements AbstractUuid {
  id: Int!
  trashed: Boolean!
  username: String!
  date: DateTime!
  lastLogin: DateTime
  description: String
  activeDonor: Boolean!
  activeAuthor: Boolean!
}

extend type Query {
  activeAuthors: [User!]!
  activeDonors: [User!]!
}
