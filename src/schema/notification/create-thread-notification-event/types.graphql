type CreateThreadNotificationEvent implements AbstractNotificationEvent {
  id: Int!
  instance: Instance!
  date: DateTime!
  actor: User!
  objectId: Int!
  object: AbstractUuid!
  thread: UnsupportedThread!
}
