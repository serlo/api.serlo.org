type SetUuidStateNotificationEvent implements AbstractNotificationEvent {
  id: Int!
  instance: Instance!
  date: DateTime!
  actor: User!
  objectId: Int!
  object: AbstractUuid!
  trashed: Boolean!
}
