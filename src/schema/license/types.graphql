type License implements InstanceAware {
  id: Int!
  instance: Instance!
  default: Boolean!
  title: String!
  url: String!
  content: String!
  agreement: String!
  iconHref: String!
}

extend type Query {
  license(id: Int!): License
}
