overwrite: true
# FIXME: this should using our *.graphql files after refactoring is done
schema: http://localhost:3000/graphql
generates:
  src/types.ts:
    plugins:
      - typescript
    config:
      scalars:
        DateTime: string
