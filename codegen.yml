overwrite: true
schema: src/**/*.graphql
generates:
  src/types.ts:
    plugins:
      - add:
          content: "import type { ComputeModel } from '~/model/types'"
      - typescript
      - typescript-resolvers
    config:
      contextType: internals/graphql/context#Context
      defaultMapper: ComputeModel<{T}>
      scalars:
        DateTime: string
        JSON: unknown
        JSONObject: 'Record<string, unknown>'
      useTypeImports: true
