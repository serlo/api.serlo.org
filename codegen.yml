overwrite: true
# FIXME: this should using our *.graphql files after refactoring is done
schema: src/**/*.graphql
generates:
  src/types.ts:
    plugins:
      - typescript
    config:
      scalars:
        DateTime: string
        JSON: unknown
        JSONObject: 'Record<string, unknown>'
