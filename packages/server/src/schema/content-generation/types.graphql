extend type Query {
  contentGeneration: ContentGenerationQuery!
}

type ContentGenerationQuery {
  generateContent(prompt: String!): ContentGenerationQueryResponse!
}

type ContentGenerationQueryResponse {
  success: Boolean!
  record: String!
}
