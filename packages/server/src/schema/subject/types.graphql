extend type Query {
  subject: SubjectQuery!
}

type SubjectQuery {
  subjects(instance: Instance!): [Subject!]!
}

type Subject {
  id: String!
  taxonomyTerm: TaxonomyTerm!
  unrevisedEntities(first: Int, after: String): AbstractEntityConnection!
}
