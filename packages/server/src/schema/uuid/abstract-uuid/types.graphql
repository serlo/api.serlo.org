interface AbstractUuid {
  id: Int!
  trashed: Boolean!
  alias: String!
  title: String!
}

type AbstractUuidConnection {
  nodes: [AbstractUuid!]!
  totalCount: Int!
  pageInfo: PageInfo!
}

type Query {
  uuid(alias: AliasInput, id: Int): AbstractUuid
}

extend type Mutation {
  uuid: UuidMutation!
}

type UuidMutation {
  setState(input: UuidSetStateInput!): DefaultResponse!
}

input UuidSetStateInput {
  id: [Int!]!
  trashed: Boolean!
}
