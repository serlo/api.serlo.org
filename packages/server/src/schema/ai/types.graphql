scalar JSONObject

extend type Query {
  ai: AiQuery!
}

type AiQuery {
  executePrompt(prompt: String!): ExecutePromptResponse!
}

type ExecutePromptResponse {
  success: Boolean!
  record: JSONObject!
}
