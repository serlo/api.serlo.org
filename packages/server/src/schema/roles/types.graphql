type ScopedRole {
  role: Role!
  scope: String
}

enum Role {
  guest
  login
  sysadmin
  moderator
  reviewer
  architect
  static_pages_builder
  admin
}

type ScopedRoleConnection {
  nodes: [ScopedRole!]!
  totalCount: Int!
  pageInfo: PageInfo!
}
