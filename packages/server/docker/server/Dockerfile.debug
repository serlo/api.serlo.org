FROM node:18
RUN apt update && apt install -y nano vim git
RUN git clone https://github.com/serlo/api.serlo.org.git

WORKDIR /api.serlo.org

RUN yarn install

ENTRYPOINT [ "yarn", "start:server" ]

EXPOSE 3000